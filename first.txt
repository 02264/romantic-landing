import React from "react";
import { motion } from "framer-motion";

// RomanticLanding.jsx
// Single-file React component (Tailwind CSS required in the project)
// - Uses Tailwind utility classes
// - Uses framer-motion for subtle entrance animations
// - Includes CSS keyframes for floating hearts and soft parallax

export default function RomanticLanding() {
  return (
    <div className="min-h-screen bg-gradient-to-b from-pink-50 via-rose-50 to-white flex flex-col items-center text-gray-800">
      {/* Decorative floating hearts (absolute) */}
      <div className="pointer-events-none fixed inset-0 -z-10 overflow-hidden">
        <div className="absolute inset-0 transform-gpu animate-clouds opacity-30"></div>
        <HeartCloud />
      </div>

      <header className="w-full max-w-5xl px-6 md:px-12 py-8 flex justify-between items-center">
        <div className="flex items-center gap-3">
          <div className="w-12 h-12 rounded-full bg-gradient-to-tr from-rose-400 to-pink-500 shadow-lg flex items-center justify-center text-white text-xl font-semibold">❤</div>
          <div>
            <h1 className="text-lg font-semibold">Mahdi & Emilisa</h1>
            <p className="text-sm text-gray-500">A little romantic corner on the web</p>
          </div>
        </div>
        <nav className="hidden md:flex items-center gap-6 text-sm text-gray-600">
          <a className="hover:text-rose-500" href="#story">Our Story</a>
          <a className="hover:text-rose-500" href="#gallery">Gallery</a>
          <a className="hover:text-rose-500" href="#message">Message</a>
        </nav>
      </header>

      <main className="flex-1 w-full flex flex-col items-center">
        <section className="w-full max-w-4xl px-6 md:px-12 py-12 text-center">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
          >
            <h2 className="text-4xl md:text-5xl font-extrabold leading-tight text-rose-600">
              For Emilisa — a page made with love
            </h2>
            <p className="mt-4 text-lg text-gray-600 max-w-2xl mx-auto">
              Soft lights, floating hearts and little animations to celebrate what you have together. Click the hearts, send a message — make it yours.
            </p>

            <div className="mt-8 flex justify-center gap-4">
              <a href="#message" className="inline-flex items-center gap-3 px-6 py-3 rounded-full bg-rose-500 text-white font-medium shadow-lg hover:scale-105 transform transition">
                Send Emilisa a love note
              </a>
              <a href="#gallery" className="inline-flex items-center gap-2 px-5 py-3 rounded-full border border-rose-200 text-rose-600 hover:bg-rose-50 transition">
                See the gallery
              </a>
            </div>
          </motion.div>

          <div className="mt-12 flex justify-center">
            <FloatingHeartsInteractive />
          </div>
        </section>

        <section id="story" className="w-full max-w-4xl px-6 md:px-12 py-12">
          <motion.div
            initial={{ opacity: 0, x: -20 }}
            whileInView={{ opacity: 1, x: 0 }}
            viewport={{ once: true }}
            transition={{ duration: 0.7 }}
            className="bg-white/80 backdrop-blur rounded-2xl p-8 shadow-md"
          >
            <h3 className="text-2xl font-bold text-rose-600">Our Story</h3>
            <p className="mt-3 text-gray-700">A short, sweet timeline: how you met, the little vacations, those small moments — keep this part personal and simple. Use short paragraphs and a couple of photos to make it warm.</p>

            <div className="mt-6 grid grid-cols-1 md:grid-cols-3 gap-4">
              <Card title="First Meeting" text="A small cafe, a rainy morning, two nervous smiles." />
              <Card title="First Trip" text="Sunrise on the coast — music, coffee, laughter." />
              <Card title="Always" text="Little gestures, shared playlists, and home-cooked dinners." />
            </div>
          </motion.div>
        </section>

        <section id="gallery" className="w-full max-w-4xl px-6 md:px-12 py-12">
          <h3 className="text-2xl font-bold text-rose-600 text-center">Gallery</h3>
          <p className="text-center text-gray-600 mt-2">A few favourite moments</p>

          <div className="mt-6 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
            {/* Example placeholders - replace with real images */}
            <GalleryItem label="Beach" />
            <GalleryItem label="Cafe" />
            <GalleryItem label="Sunset" />
          </div>
        </section>

        <section id="message" className="w-full max-w-4xl px-6 md:px-12 py-12">
          <div className="bg-white/90 backdrop-blur rounded-2xl p-8 shadow-md">
            <h3 className="text-2xl font-bold text-rose-600">Send a message to Emilisa</h3>
            <p className="text-gray-600 mt-2">Write something sweet — it will only be displayed here (or you can wire this form to an email endpoint).</p>

            <form className="mt-6 grid grid-cols-1 md:grid-cols-2 gap-4">
              <FloatingInput name="name" placeholder="Your name" />
              <FloatingInput name="email" placeholder="Your email (optional)" />
              <div className="md:col-span-2">
                <label className="block text-sm text-gray-600 mb-2">Message</label>
                <textarea className="w-full rounded-xl border border-rose-100 p-3 focus:outline-none focus:ring-2 focus:ring-rose-200" rows={4} placeholder="Write something from the heart..."></textarea>
                <div className="mt-4 flex justify-end">
                  <button type="button" className="px-5 py-2 rounded-full bg-rose-500 text-white font-medium shadow hover:scale-105 transform transition">Send</button>
                </div>
              </div>
            </form>
          </div>
        </section>
      </main>

      <footer className="w-full max-w-5xl px-6 py-6 text-center text-sm text-gray-500">
        Made with ♥ for Emilisa — by Mahdi
      </footer>

      {/* Inline styles and SVGs */}
      <style jsx>{`
        @keyframes floatUp {
          0% { transform: translateY(0) }
          50% { transform: translateY(-18px) }
          100% { transform: translateY(0) }
        }

        @keyframes drift {
          0% { transform: translateX(0) }
          50% { transform: translateX(18px) }
          100% { transform: translateX(0) }
        }

        .heart {
          animation: floatUp 4s ease-in-out infinite;
        }

        .heart.delay-1 { animation-delay: 0.3s }
        .heart.delay-2 { animation-delay: 0.7s }
        .heart.delay-3 { animation-delay: 1.1s }

        .animate-clouds {
          background: radial-gradient(circle at 20% 10%, rgba(255,200,210,0.08), transparent 10%),
                      radial-gradient(circle at 80% 70%, rgba(255,180,200,0.06), transparent 12%);
          animation: drift 30s linear infinite;
        }

        /* responsive tweaks for the interactive hearts */
        @media (max-width: 640px) {
          h2 { font-size: 2rem }
        }
      `}</style>
    </div>
  );
}

function HeartCloud() {
  // Decorative hearts floating in the background
  const hearts = new Array(10).fill(0).map((_, i) => (
    <svg key={i} className={`absolute ${i % 2 ? 'left-1/6' : 'left-4/6'} top-${(i * 7) % 100}`} width="48" height="48" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M12 21s-8-6.5-8-11.5S7 2 12 6.5 20 2 20 9.5 12 21 12 21z" fill="rgba(236,72,153,0.07)"/>
    </svg>
  ));
  return <div className="w-full h-full">{hearts}</div>;
}

function FloatingHeartsInteractive() {
  return (
    <div className="relative w-96 h-48 flex items-center justify-center">
      <button className="absolute -left-8 heart delay-1 w-12 h-12 rounded-full flex items-center justify-center text-white text-xl shadow-lg bg-rose-400/90">❤</button>
      <motion.div animate={{ scale: [1, 1.05, 1] }} transition={{ repeat: Infinity, duration: 3 }} className="w-64 h-36 rounded-2xl bg-gradient-to-tr from-white to-pink-50 shadow-2xl flex items-center justify-center">
        <div className="text-center px-6">
          <p className="font-medium text-rose-600">Click the hearts</p>
          <p className="mt-2 text-sm text-gray-500">Tiny interactions make big memories.</p>
        </div>
      </motion.div>
      <button className="absolute -right-8 heart delay-2 w-12 h-12 rounded-full flex items-center justify-center text-white text-xl shadow-lg bg-pink-500/90">❤</button>
    </div>
  );
}

function Card({ title, text }) {
  return (
    <div className="rounded-xl p-4 bg-white/80 backdrop-blur shadow-inner">
      <h4 className="font-semibold text-rose-600">{title}</h4>
      <p className="text-sm text-gray-600 mt-2">{text}</p>
    </div>
  );
}

function GalleryItem({ label }) {
  return (
    <div className="rounded-xl overflow-hidden bg-gradient-to-tr from-pink-50 to-white shadow-md p-6 flex items-end" style={{ minHeight: 160 }}>
      <div>
        <div className="text-sm font-semibold text-rose-600">{label}</div>
        <div className="text-xs text-gray-500 mt-1">Tap to open</div>
      </div>
    </div>
  );
}

function FloatingInput({ name, placeholder }) {
  return (
    <div className="relative">
      <input id={name} name={name} placeholder=" " className="peer w-full rounded-xl border border-rose-100 p-4 focus:outline-none focus:ring-2 focus:ring-rose-200" />
      <label htmlFor={name} className="absolute left-4 -top-2 bg-white px-2 text-xs text-rose-600 peer-placeholder-shown:top-3 peer-placeholder-shown:text-sm peer-placeholder-shown:bg-transparent transition-all">{placeholder}</label>
    </div>
  );
}
